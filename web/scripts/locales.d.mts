/** Every locale that has a file in src/i18n/locales, sorted. See locales.mjs. */
export declare function localeCodes(): string[];

/** The locale to assume before anything is known about the reader. See locales.mjs. */
export declare const DEFAULT_LOCALE: string;
