<?xml version="1.0" encoding="utf-8"?>
<!-- The unticked state: `variant="outline"` - a bordered pill on the card, not a filled one. The
     two states differ by fill rather than by wording, exactly as they do on the day card, so the
     button never turns into a congratulation and its width never changes as it is pressed. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="999dp" />
    <stroke android:width="1dp" android:color="@color/widget_border" />
</shape>
